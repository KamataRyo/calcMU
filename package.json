{
  "name": "calcMU",
  "version": "1.0.0",
  "description": "calc MUs(ingress Score) of polygon. Japan local only",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KamataRyo",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-mocha": "^2.0.1",
    "should": "^5.2.0"
  }
}
